[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  node_bundler = "esbuild"
  # Keep OpenAI out of the bundle to avoid ESM/CJS issues
  external_node_modules = ["openai"]
